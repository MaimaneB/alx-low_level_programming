#include "main.h"
#include <stdio.h>

/**
* _isdigit - checks whether the character is a digit or not 
* @c: tested character 
* Return: 0
*/

int _isdigit(int c)
{
	if ((c >= 48) && (c <= 57))
	{
		retunr (1);
	}
return (0);
}
