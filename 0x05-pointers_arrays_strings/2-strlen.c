#include "main.h"

/**
* _strlen - returns the strings length
* @s: string
* Return: returns the length as an integer;
*/

int _strlen(char *s)
{
	int leng = 0;

	while (*(s + len) != '\0')
		len++;

	return (len);
}
